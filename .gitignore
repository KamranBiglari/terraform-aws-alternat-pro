# Local terraform state and plugin caches
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
crash.log
crash.*.log

# Local override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Build artefacts (Lambda zip files, rendered SFN definitions)
.build/

# Terraform plan / apply artefacts
*.tfplan

# Editor / OS noise
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
